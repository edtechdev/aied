---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const faqs = await getCollection('faqs');
const sorted = faqs.slice().sort((a, b) => (a.data.created || '').localeCompare(b.data.created || ''));
---

<BaseLayout title="FAQ">
  <h1>❓ Frequently Asked Questions</h1>

  <div style="line-height:1.7;color:var(--color);margin-bottom:1.5rem;">
    <p style="margin-bottom:0.75rem;">
      This section answers common questions about <strong>AI in education</strong> — what the research says
      about how AI affects teaching and learning, and how educators, instructors, and instructional
      designers can put that evidence into practice. Each answer distills findings from the research
      summarized across this knowledge base, connecting the question to the relevant concepts and
      articles for deeper reading.
    </p>
    <p>
      Have a question not answered here? Ask your own AI assistant using the <a href="/aied/ai" style="font-weight:500;">Use This Knowledge Base with Your Own AI Assistant</a> page, which shows how to point an agent at the full catalog for grounded, cited answers.
    </p>
  </div>

  {sorted.length === 0 && <p>No FAQs yet.</p>}

  <div class="faq-list">
    {sorted.map(f => {
      const href = `/aied/faqs/${f.id.replace('.md', '')}`;
      return (
        <a href={href} class="faq-item">
          {f.data.title}
        </a>
      );
    })}
  </div>
</BaseLayout>

<style>
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    margin-top: 1rem;
  }
  .faq-item {
    display: block;
    font-weight: 600;
    color: var(--primary);
    text-decoration: none;
    font-size: 1.05rem;
    line-height: 1.5;
    padding: 0.75rem 1rem;
    border-radius: 8px;
    border: 1px solid var(--card-border-color);
    background: color-mix(in srgb, var(--primary) 4%, transparent);
    transition: background 0.15s, color 0.15s, border-color 0.15s;
  }
  .faq-item:hover {
    background: color-mix(in srgb, var(--primary) 12%, transparent);
    border-color: var(--primary);
  }
</style>
